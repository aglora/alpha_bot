<launch>
    <!-- Cargamos archivo de configuración de RVIZ-->
    <arg name="rvizconfig" default="$(find alpha_bot_simulation)/rviz/basic_config.rviz" />

    <!-- Abrimos rviz -->
    <node name="rviz" pkg="rviz" type="rviz" args="-d $(arg rvizconfig)" required="true" />
</launch>